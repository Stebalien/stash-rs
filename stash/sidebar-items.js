initSidebarItems({"mod":[["stash",""],["unique_stash",""]],"struct":[["Stash","A `Stash` is a place to put items where you need (amortized) `O(1)` insertion, deletion, and lookups but don't care about the order of the items, don't need to be able to choose the keys, and don't want to pay the overhead of hashing."],["Tag","A versioned index into a `UniqueStash`. No two calls to `put` on the same `UniqueStash` will ever return the same `Tag`."],["UniqueStash","A `UniqueStash` is a place to put items where you need (amortized) `O(1)` insertion, deletion, and lookups but don't care about the order of the items, don't need to be able to choose the keys, don't want to pay the overhead of hashing, and can't handle key reuse."]]});